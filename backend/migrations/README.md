# Database migrations

Run `backend/migrations/001_complete_schema.sql` in the Supabase SQL Editor.

This is the single source of truth for the CallPilot AI database schema. Ensure the `documents` Supabase Storage bucket exists before document uploads are used; it should be private.
